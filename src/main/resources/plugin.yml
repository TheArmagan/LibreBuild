name: LibreBuild
version: '1.0.0'
main: net.ezplace.librebuild.LibreBuild
api-version: '1.20'
softdepend: [WorldEdit, WorldGuard]
author: andrewYerNau
commands:
  librebuild:
    aliases: [lb]
    usage: "/librebuild help for more information"
permissions:
  librebuild.item.use:
    default: true
  librebuild.command.admin:
    default: op